obj-$(CONFIG_VENDER_PSV_LEGACY) := fimc-is-vender.o \

obj-$(CONFIG_PSV_SFR_VERIFICATION) += fimc-is-sfr.o
obj-$(CONFIG_PSV_VECTOR_VERIFICATION) += fimc-is-vector.o
obj-$(CONFIG_PSV_ASB) += asb/

EXTRA_CFLAGS += -Idrivers/media/platform/exynos/fimc-legacy
EXTRA_CFLAGS += -Idrivers/media/platform/exynos/fimc-legacy/include
